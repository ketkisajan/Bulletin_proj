<p class="bg-primary text-center">User Details</p>


<div align="center">
  

 <%= will_paginate %>  


  <% @users.each do |user| %>
  <% if (logged_in? && user == current_user) || (logged_in? && current_user.admin?)%> 
    <ul class="listing">
      <div class="row">
        <div class="well col-md-4 col-md-offset-4">
          <li class="article-title">
            <%= link_to user.username, user_path(user) %>
          </li>
          <li><small><%= pluralize(user.subscriber_lists.count, "subscriber_list") if user.subscriber_lists %></small></li>
         

 

  <li><%= link_to "Edit user", edit_user_path(user), class: "btn btn-outline-info"%></li>



      <li><%= link_to "Delete user", user_path(user), method: :delete, data: {confirm: "Are you sure you want to delete the user and all associated subscriber lists?"} %></li>
        
 </div>
        </div>
  
    </ul>
<% end %> 

       
  <% end %>



 <%= will_paginate %>  

</div>